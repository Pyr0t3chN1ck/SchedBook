import * as AppointmentsActions from './appointments.actions';

describe('Appointments', () => {
  it('should create an instance', () => {
    expect(new AppointmentsActions.LoadAppointmentss()).toBeTruthy();
  });
});
